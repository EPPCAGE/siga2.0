name: CI - Informational

on:
  push:
    branches: [ main, 'feature/**', 'pr/**' ]
  pull_request:
    branches: [ main ]

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Project Info
        run: echo "CI/CD pipeline running - scanners are informational only"